[project]
name = "xymphony-contracts"
version = "0.1.0"
description = "Shared Pydantic domain contracts for Xymphony Voice"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
